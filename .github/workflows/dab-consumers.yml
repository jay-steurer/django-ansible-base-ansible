---
name: Testing DAB Consumers
on:
  push:
  pull_request:
jobs:
  awx:
    name: AWX
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
    strategy:
      fail-fast: false
    env:
      tower_image: "quay.io/aap/tower_devel:release_4.6"
    steps:
      - uses: actions/checkout@v4
        with:
          show-progress: false

      - name: Log in to Quay
        run: |
          echo "${{ secrets.QUAY_TOKEN }}" | docker login quay.io -u ${{ vars.QUAY_USER }} --password-stdin

      - name: Pull AWX image compatible with AAP 2.5
        run: docker pull ${{ env.tower_image }}


      # Generate a docker image that includes current DAB
      - name: Create image for AWX devel that includes our DAB
        run: |
          mkdir awx-dockerfile
          cat <<EOF > awx-dockerfile/Dockerfile
          FROM ${{ env.tower_image }}

          COPY . /opt/dab/
          RUN pip install -e /opt/dab/
          RUN pip install jq
          EOF
          docker build -t awx-with-dab:latest -f awx-dockerfile/Dockerfile .

      - uses: actions/checkout@v4
        with:
          repository: ansible-automation-platform/tower
          path: awx
          ref: release_4.6
          token: ${{ secrets.ALANCODING_DOWNSTREAM_CHECKS_AAP_ORG_TOKEN }}

      - name: Run AWX tests in the generated image
        run: |
          cd awx
          DEVEL_IMAGE_NAME=awx-with-dab:latest AWX_DOCKER_CMD=/start_tests.sh make docker-runner

  eda-server:
    name: eda-server
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
    strategy:
      fail-fast: false
    env:
      EDA_SECRET_KEY: 'test'
      EDA_DB_PASSWORD: 'secret'
    services:
      postgres:
        image: 'quay.io/sclorg/postgresql-15-c9s:latest'
        env:
          POSTGRESQL_USER: eda
          POSTGRESQL_PASSWORD: secret
          POSTGRESQL_ADMIN_PASSWORD: secret
          POSTGRESQL_DATABASE: eda
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - '5432:5432'
      redis:
        image: 'quay.io/fedora/redis-6:latest'
        ports:
          - '6379:6379'
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v4
        with:
          show-progress: false

      - uses: actions/checkout@v4
        with:
          repository: ansible-automation-platform/aap-eda-controller
          ref: stable-1.1
          path: eda-server
          token: ${{ secrets.ALANCODING_DOWNSTREAM_CHECKS_AAP_ORG_TOKEN }}

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Run eda-server tests
        run: |
          DAB_PATH="$PWD"
          mv eda-server /tmp
          cd /tmp/eda-server

          pipx install poetry

          # Install local DAB first
          poetry run pip install -e "$DAB_PATH[all]"

          # Remove django-ansible-base from poetry.lock to prevent poetry from reinstalling it
          poetry remove django-ansible-base --lock || true

          # Install remaining dependencies
          poetry install -E all --only main,test

          poetry run pip show django-ansible-base
          poetry run pytest
